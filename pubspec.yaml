name: firestore_access_policy
description: >
  Declarative Firestore access policies in Dart — generate security rules,
  tests, and optional client guards from a single CRUD + membership model.
version: 0.1.0
homepage: https://github.com/Laplace-Apps/firestore_access_policy
repository: https://github.com/Laplace-Apps/firestore_access_policy
issue_tracker: https://github.com/Laplace-Apps/firestore_access_policy/issues

environment:
  sdk: ^3.10.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.14
